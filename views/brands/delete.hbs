{{#extends 'base'}}

{{#block 'title'}}
Brands
{{/block}}

{{#block 'content'}}

<header class="">
    <h1>Delete Brand: {{brand.name}}</h1>
</header>

<form method="POST">
    <input type="hidden" name="_csrf" value="{{csrfToken}}"/>
    <div class="alert alert-danger">
        <p>Are you sure you want to delete {{brand.name}}?</p> 
        <input type="submit" value="Yes" class="btn btn-danger"/>
        <a href="/brands" class="btn btn-success">No</a>
    </div>
</form>

{{/block}}

{{/extends}}